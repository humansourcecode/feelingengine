"""Rendering — visual output from the Feeling Engine vocabulary.

Produces brain images (and other visuals) from mechanism labels without
requiring TRIBE at runtime. Uses canonical activation signatures per label
(our IP) + public brain atlas infrastructure (fsaverage + HCP-MMP).
"""
